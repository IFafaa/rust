pub fn panic() {
    // panic! macro
    // The panic! macro is used to trigger a panic in Rust. It can be used to indicate an unrecoverable error or a condition that should never occur.
    // The panic! macro takes a format string and optional arguments, similar to the println! macro.
    // When a panic occurs, the program will terminate and print an error message to the console.
    panic!("This is a panic message");

    // The panic! macro can also be used to trigger a panic with a custom message.
    // This is useful for debugging and error handling.
    
}   